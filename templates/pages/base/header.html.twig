<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home')}}"> Mon Agence</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page"  {% if current_menu is defined and current_menu == 'properties' %} active {% endif %} href="{{path('property.index')}}">Acheter</a>
        </li>
      </ul>
        {% if app.user %}
              <ul class = "navbar-nav my-2 my-lg-0 ">
                    <li class="nav-item">
                      <a href="{{ path('app_logout') }}" class="nav-link"> Deconnexion </a>
                    </li>
              </ul>
       {% endif %}
      
    </div> 
  </div>

</nav>
